/*
package com.kashish.product.pcos.adapter

import android.content.Context
import android.support.v7.widget.RecyclerView
import android.view.LayoutInflater
import android.view.ViewGroup
import com.kashish.product.pcos.R

//df.columns=['time','diaganosed','result','age','overweight','weightgain','periods','conceiving','chinHair',
// 'cheeksHair','upperLipHair','betweenBreastHair','armsHair','innerThighHair','acneOrskinTag','hairThinning',
// 'darkPatch','tiredness','moodSwings','exercise','eatOutside','cannedFood','city']

class QuestionsAdapter(var context: Context) : RecyclerView.Adapter<RecyclerView.ViewHolder>() {
    override fun onCreateViewHolder(p0: ViewGroup, p1: Int): RecyclerView.ViewHolder {
        if (p1 == 1){
            val v = LayoutInflater.from(context).inflate(R.layout.question_one,p0,false)
            return ViewHolderOne(v)
        }else
            throw RuntimeException("The type has to be ONE or TWO")
    }

    override fun getItemCount(): Int {
        return
    }

    override fun onBindViewHolder(p0: RecyclerView.ViewHolder, p1: Int) {
    }
}*/
